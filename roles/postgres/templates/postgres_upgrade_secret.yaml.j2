# Postgres Secret.
---
apiVersion: v1
kind: Secret
metadata:
  name: '{{ ansible_operator_meta.name }}-postgres-configuration'
  namespace: '{{ ansible_operator_meta.namespace }}'
  labels:
    {{ lookup("template", "../common/templates/labels/common.yaml.j2") | indent(width=4) | trim }}
stringData:
  password: '{{ eda_postgres_pass }}'
  username: '{{ eda_postgres_user }}'
  database: '{{ eda_postgres_database }}'
  port: '{{ eda_postgres_port }}'
  host: '{{ ansible_operator_meta.name }}-postgres-{{ supported_pg_version }}'
  type: 'managed'

