---
apiVersion: v1
kind: Service
metadata:
  name: "{{ ansible_operator_meta.name }}-postgres"
  namespace: "{{ ansible_operator_meta.namespace }}"
  labels:
    app: eda
    comp: postgres
spec:
  ports:
  - name: "5432"
    port: 5432
    targetPort: 5432
  clusterIP: None
  selector:
    app: eda
    comp: postgres
status:
  loadBalancer: {}
